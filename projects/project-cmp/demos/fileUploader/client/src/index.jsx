import React from 'react';
import ReactDOM from "react-dom/client";
import UpLoadFile from './Upload';

const root = document.getElementById("root");

ReactDOM.createRoot(root).render(<UpLoadFile />);