import { drawObjectGuides } from "./guideLineService";

export const addGroup = (object, text, canvas, options) => {
  return {};
}

// 1. 解开group
// 2. focus文本框
const handleDblclick = (group, canvas, text) => {

}

// 1. 移除原来的group
// 2. 将所有group的子项单一渲染
const divideGroupToRender = (group, canvas) => {
  return {};
}

const generateGroup = (items, canvas, text, option) => {
  return {};
}
